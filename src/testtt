
       <!-- <Select>
      <SelectTrigger className="w-[180px]">
        <SelectValue placeholder="Select a fruit" />
      </SelectTrigger>
      <SelectContent>
        <SelectGroup>
          <SelectLabel>Fruits</SelectLabel>
          { data?.map((product) => {
            return <SelectItem key={product.id} value="apple">{product.name}</SelectItem>
           })}
        </SelectGroup>
      </SelectContent>
    </Select> -->       